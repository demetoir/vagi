import sinon from "sinon";
import assert from "assert";
import {describe, it} from "mocha";

describe("RedisJSONCache", () => {
	const onMissSpy = sinon.spy();

	// todo test
	it("construct", () => {
		assert(true);
	});

	describe("set", () => {
		// todo test
		it("should ", () => {});
	});
	describe("get", () => {
		// todo test
		it("on hit ", () => {});
		// todo test
		it("on miss ", () => {});
	});
});
